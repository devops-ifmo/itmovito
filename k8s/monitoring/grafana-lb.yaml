apiVersion: v1
kind: Service
metadata:
  name: grafana-lb
  namespace: monitoring
spec:
  selector:
    app.kubernetes.io/name: grafana
    app.kubernetes.io/instance: kps
  type: LoadBalancer
  ports:
    - name: http
      port: 80
      targetPort: grafana
      nodePort: 30280
